import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: [
      { find: '@', replacement: '/opt/render/project/src/client/src' },
      { find: '@shared', replacement: '/opt/render/project/src/shared' }
    ]
  },
  build: {
    outDir: "../dist/public",
    emptyOutDir: true,
  },
});
